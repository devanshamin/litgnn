model_cls: PNA
hidden_channels: 64
out_channels: ???
num_conv_layers: 3
dropout: 0.0
towers: 4 # No. of tower containing linear layers (pre_layers & post_layers)
pre_layers: 1 # No. of linear layers before aggegration
post_layers: 1 # No. of linear layers after aggegration
deg: null # Will be automatically populated
aggregators: ["mean", "min", "max", "std"]
scalers: ["identity", "amplification", "attenuation"]